eureka:
 instance:
   prefer-ip-address: true
 client:
   fetch-registry: true
   register-with-eureka: true
   service-url:
     defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}
cards:
  url: ${CARDS_URL:http://localhost:8082}
accounts:
   url: ${ACCOUNTS_URL:http://localhost:8081}